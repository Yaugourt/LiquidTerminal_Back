# MISSION-08 — HIP-3 user scoped (3 endpoints)

## Endpoints

1. `GET /hip3/users/{address}/coins`
2. `GET /hip3/users/{address}/fills`
3. `GET /hip3/users/{address}/overview`

## Livrables

- Validation `address` en path
- Routes `/indexer/hip3/users/:address/...`
